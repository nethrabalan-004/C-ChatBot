{
  "name": "career-guidance-backend",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build-frontend": "cd ../frontend && npm install && npm run build && cd ../backend && rm -rf public && mkdir -p public && cp -r ../frontend/build/* public/"
  },  
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "rimraf": "^6.0.1"
  }
}
